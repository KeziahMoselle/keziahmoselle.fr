{
  "name": "keziahmoselle.fr",
  "version": "2.0.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.2.0",
    "ky": "0.5.2",
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-scripts": "2.1.5",
    "react-spring": "^8.0.7"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "http-proxy-middleware": "^0.19.1",
    "netlify-lambda": "^1.4.2"
  },
  "scripts": {
    "start": "concurrently \"react-scripts start\" \"netlify-lambda serve src/lambda\"",
    "build": "npm run build:react && npm run build:lambda",
    "build:react": "react-scripts build",
    "build:lambda": "netlify-lambda build src/lambda",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
